import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.examenfinalphibridaappavisos',
  appName: 'examenfinalphibridaappavisos',
  webDir: 'www'
};

export default config;
